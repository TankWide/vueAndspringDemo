server:
  port: 8080

spring:
  application:
    name: springboot-demo

  #使用环境
  profiles:
    active: dev
    # 配置中心
  cloud:
    config:
      fail-fast: true
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
      username: upop-config
      password: S1oebaTzYWA3soky
      discovery:
        enabled: true
        service-id: upop-config
---
spring:
  profiles: dev

  web:
    upload-path: /usr/local/szacrm/web/upload/
    vue-path: /usr/local/szacrm/web/

