<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.BigCustomMapper">

    <select id="pagination" resultType="com.example.demo.dao.entity.DiaryEntity">
        SELECT
        ci.ID AS id,
        ci.DIARY_HEAD AS diaryHead,
        ci.DIARY_CONTENT AS diaryContent
        FROM
        DIARY ci
    </select>

    <insert id="addDiary" parameterType="com.example.demo.dao.entity.DiaryEntity">
        INSERT INTO DIARY(DIARY_HEAD,DIARY_CONTENT)
        VALUES(#{diaryHead},#{diaryContent})
    </insert>

</mapper>
